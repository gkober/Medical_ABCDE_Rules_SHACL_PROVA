In order to execute the SHACL rules:

- Copy the file WorkflowRunnerResult_correctResults.csv in this folder.
- Unzip the file 1000_samples.zip into a subfolder "1000_samples".
- Compile executeABCDE.java (in Windows, just double-click on compile.bat)
- Run executeABCDE.class (in Windows, just double-click on run.bat)